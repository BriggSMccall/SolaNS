.anchor
.DS_Store
target
**/*.rs.bk
node_modules
test-ledger
.yarn
.surfpool

# JS / TS
dist
*.tsbuildinfo
.env
.env.local
coverage
.pnpm-store

# Web app + deploy config live in a separate project (../SolaNS-site), not this repo
web
.vercel
.vercelignore
vercel.json
scratch-deploy

# Playwright CLI session artifacts
.playwright-cli
