/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getI64Decoder,
  getI64Encoder,
  getOptionDecoder,
  getOptionEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type Account,
  type Address,
  type Codec,
  type Decoder,
  type EncodedAccount,
  type Encoder,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type MaybeAccount,
  type MaybeEncodedAccount,
  type Option,
  type OptionOrNullable,
  type ReadonlyUint8Array,
} from "@solana/kit";

export const AUCTION_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  218, 94, 247, 242, 126, 233, 131, 81,
]);

export function getAuctionDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(AUCTION_DISCRIMINATOR);
}

export type Auction = {
  discriminator: ReadonlyUint8Array;
  /** The seller — receives the winning bid (minus fee) + the rent at settle. */
  seller: Address;
  /** The auctioned name's hash (matches `NameRecord.name_hash`). */
  nameHash: ReadonlyUint8Array;
  /** The PDA-owned `$SOLANS` ATA holding the current highest bid. */
  bidVault: Address;
  /** Current highest bidder, or `None` until the first bid. */
  highestBidder: Option<Address>;
  /** Current highest bid (`$SOLANS` base units); `0` until the first bid. */
  highestBid: bigint;
  /** Minimum acceptable first bid (`$SOLANS` base units). */
  reservePrice: bigint;
  /** Minimum raise over the current highest bid. */
  minIncrement: bigint;
  /** Unix timestamp the auction closes (extended by late bids). */
  endTime: bigint;
  /** Canonical PDA bump. */
  bump: number;
};

export type AuctionArgs = {
  /** The seller — receives the winning bid (minus fee) + the rent at settle. */
  seller: Address;
  /** The auctioned name's hash (matches `NameRecord.name_hash`). */
  nameHash: ReadonlyUint8Array;
  /** The PDA-owned `$SOLANS` ATA holding the current highest bid. */
  bidVault: Address;
  /** Current highest bidder, or `None` until the first bid. */
  highestBidder: OptionOrNullable<Address>;
  /** Current highest bid (`$SOLANS` base units); `0` until the first bid. */
  highestBid: number | bigint;
  /** Minimum acceptable first bid (`$SOLANS` base units). */
  reservePrice: number | bigint;
  /** Minimum raise over the current highest bid. */
  minIncrement: number | bigint;
  /** Unix timestamp the auction closes (extended by late bids). */
  endTime: number | bigint;
  /** Canonical PDA bump. */
  bump: number;
};

/** Gets the encoder for {@link AuctionArgs} account data. */
export function getAuctionEncoder(): Encoder<AuctionArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["seller", getAddressEncoder()],
      ["nameHash", fixEncoderSize(getBytesEncoder(), 32)],
      ["bidVault", getAddressEncoder()],
      ["highestBidder", getOptionEncoder(getAddressEncoder())],
      ["highestBid", getU64Encoder()],
      ["reservePrice", getU64Encoder()],
      ["minIncrement", getU64Encoder()],
      ["endTime", getI64Encoder()],
      ["bump", getU8Encoder()],
    ]),
    (value) => ({ ...value, discriminator: AUCTION_DISCRIMINATOR }),
  );
}

/** Gets the decoder for {@link Auction} account data. */
export function getAuctionDecoder(): Decoder<Auction> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["seller", getAddressDecoder()],
    ["nameHash", fixDecoderSize(getBytesDecoder(), 32)],
    ["bidVault", getAddressDecoder()],
    ["highestBidder", getOptionDecoder(getAddressDecoder())],
    ["highestBid", getU64Decoder()],
    ["reservePrice", getU64Decoder()],
    ["minIncrement", getU64Decoder()],
    ["endTime", getI64Decoder()],
    ["bump", getU8Decoder()],
  ]);
}

/** Gets the codec for {@link Auction} account data. */
export function getAuctionCodec(): Codec<AuctionArgs, Auction> {
  return combineCodec(getAuctionEncoder(), getAuctionDecoder());
}

export function decodeAuction<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>,
): Account<Auction, TAddress>;
export function decodeAuction<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>,
): MaybeAccount<Auction, TAddress>;
export function decodeAuction<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>,
): Account<Auction, TAddress> | MaybeAccount<Auction, TAddress> {
  return decodeAccount(
    encodedAccount as MaybeEncodedAccount<TAddress>,
    getAuctionDecoder(),
  );
}

export async function fetchAuction<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<Account<Auction, TAddress>> {
  const maybeAccount = await fetchMaybeAuction(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeAuction<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<MaybeAccount<Auction, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeAuction(maybeAccount);
}

export async function fetchAllAuction(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<Account<Auction>[]> {
  const maybeAccounts = await fetchAllMaybeAuction(rpc, addresses, config);
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeAuction(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<MaybeAccount<Auction>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) => decodeAuction(maybeAccount));
}
