/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  addDecoderSizePrefix,
  addEncoderSizePrefix,
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getArrayDecoder,
  getArrayEncoder,
  getBooleanDecoder,
  getBooleanEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getI64Decoder,
  getI64Encoder,
  getOptionDecoder,
  getOptionEncoder,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getU8Decoder,
  getU8Encoder,
  getUtf8Decoder,
  getUtf8Encoder,
  transformEncoder,
  type Account,
  type Address,
  type Codec,
  type Decoder,
  type EncodedAccount,
  type Encoder,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type MaybeAccount,
  type MaybeEncodedAccount,
  type Option,
  type OptionOrNullable,
  type ReadonlyUint8Array,
} from "@solana/kit";
import {
  getRecordDecoder,
  getRecordEncoder,
  type Record,
  type RecordArgs,
} from "../types";

export const NAME_RECORD_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  254, 22, 17, 161, 229, 49, 238, 105,
]);

export function getNameRecordDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(NAME_RECORD_DISCRIMINATOR);
}

export type NameRecord = {
  discriminator: ReadonlyUint8Array;
  /** Current owner of the name. */
  owner: Address;
  /** Optional delegate allowed to manage records (not transfer/ownership). */
  controller: Option<Address>;
  /** keccak256(name + "." + tld) — the canonical key (also the PDA seed). */
  nameHash: ReadonlyUint8Array;
  /** Top-level domain, e.g. "sol". */
  tld: string;
  /** Unix timestamp of (most recent) registration. */
  registeredAt: bigint;
  /** Unix timestamp of expiration. */
  expiresAt: bigint;
  /** Arbitrary key -> value records. */
  records: Array<Record>;
  /** Optional custom resolver program (set via `set_resolver`). */
  resolver: Option<Address>;
  /** Optional hosting content ref / CID (set via `set_hosting`). */
  hostingRef: Option<string>;
  /** When true, `transfer_name` is rejected. */
  transferLocked: boolean;
  /** Whether the owner has set a reverse record pointing here. */
  reverseSet: boolean;
  /**
   * The NFT mint when the name is tokenized (`Some`), else `None`. While
   * tokenized, holdership of this mint — not the `owner` field — is what
   * authorizes record-level changes; structural changes require `redeem_name`
   * first. The PDA remains the canonical record (expiry, records live here).
   */
  nftMint: Option<Address>;
  /**
   * The parent name's PDA when this is a subdomain (`Some`), else `None` for a
   * top-level name. Resolution follows this pointer up to the root.
   */
  parent: Option<Address>;
  /**
   * The parent's `registered_at` captured at creation. Resolution requires it
   * still equals the live parent's `registered_at`; a claim or burn+re-register
   * rewrites the parent's `registered_at` and so invalidates the whole subtree.
   */
  parentRegisteredAt: bigint;
  /** Subdomain depth: 0 for a top-level name, +1 per level (capped on creation). */
  depth: number;
  /**
   * When true, the name is listed for sale on the native marketplace and is
   * frozen: owner-gated mutations are rejected until `buy_name`/`cancel_listing`.
   * Non-custodial — the seller keeps `owner`; the lock is what protects buyers.
   */
  listed: boolean;
  /** Canonical PDA bump. */
  bump: number;
};

export type NameRecordArgs = {
  /** Current owner of the name. */
  owner: Address;
  /** Optional delegate allowed to manage records (not transfer/ownership). */
  controller: OptionOrNullable<Address>;
  /** keccak256(name + "." + tld) — the canonical key (also the PDA seed). */
  nameHash: ReadonlyUint8Array;
  /** Top-level domain, e.g. "sol". */
  tld: string;
  /** Unix timestamp of (most recent) registration. */
  registeredAt: number | bigint;
  /** Unix timestamp of expiration. */
  expiresAt: number | bigint;
  /** Arbitrary key -> value records. */
  records: Array<RecordArgs>;
  /** Optional custom resolver program (set via `set_resolver`). */
  resolver: OptionOrNullable<Address>;
  /** Optional hosting content ref / CID (set via `set_hosting`). */
  hostingRef: OptionOrNullable<string>;
  /** When true, `transfer_name` is rejected. */
  transferLocked: boolean;
  /** Whether the owner has set a reverse record pointing here. */
  reverseSet: boolean;
  /**
   * The NFT mint when the name is tokenized (`Some`), else `None`. While
   * tokenized, holdership of this mint — not the `owner` field — is what
   * authorizes record-level changes; structural changes require `redeem_name`
   * first. The PDA remains the canonical record (expiry, records live here).
   */
  nftMint: OptionOrNullable<Address>;
  /**
   * The parent name's PDA when this is a subdomain (`Some`), else `None` for a
   * top-level name. Resolution follows this pointer up to the root.
   */
  parent: OptionOrNullable<Address>;
  /**
   * The parent's `registered_at` captured at creation. Resolution requires it
   * still equals the live parent's `registered_at`; a claim or burn+re-register
   * rewrites the parent's `registered_at` and so invalidates the whole subtree.
   */
  parentRegisteredAt: number | bigint;
  /** Subdomain depth: 0 for a top-level name, +1 per level (capped on creation). */
  depth: number;
  /**
   * When true, the name is listed for sale on the native marketplace and is
   * frozen: owner-gated mutations are rejected until `buy_name`/`cancel_listing`.
   * Non-custodial — the seller keeps `owner`; the lock is what protects buyers.
   */
  listed: boolean;
  /** Canonical PDA bump. */
  bump: number;
};

/** Gets the encoder for {@link NameRecordArgs} account data. */
export function getNameRecordEncoder(): Encoder<NameRecordArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["owner", getAddressEncoder()],
      ["controller", getOptionEncoder(getAddressEncoder())],
      ["nameHash", fixEncoderSize(getBytesEncoder(), 32)],
      ["tld", addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
      ["registeredAt", getI64Encoder()],
      ["expiresAt", getI64Encoder()],
      ["records", getArrayEncoder(getRecordEncoder())],
      ["resolver", getOptionEncoder(getAddressEncoder())],
      [
        "hostingRef",
        getOptionEncoder(
          addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder()),
        ),
      ],
      ["transferLocked", getBooleanEncoder()],
      ["reverseSet", getBooleanEncoder()],
      ["nftMint", getOptionEncoder(getAddressEncoder())],
      ["parent", getOptionEncoder(getAddressEncoder())],
      ["parentRegisteredAt", getI64Encoder()],
      ["depth", getU8Encoder()],
      ["listed", getBooleanEncoder()],
      ["bump", getU8Encoder()],
    ]),
    (value) => ({ ...value, discriminator: NAME_RECORD_DISCRIMINATOR }),
  );
}

/** Gets the decoder for {@link NameRecord} account data. */
export function getNameRecordDecoder(): Decoder<NameRecord> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["owner", getAddressDecoder()],
    ["controller", getOptionDecoder(getAddressDecoder())],
    ["nameHash", fixDecoderSize(getBytesDecoder(), 32)],
    ["tld", addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
    ["registeredAt", getI64Decoder()],
    ["expiresAt", getI64Decoder()],
    ["records", getArrayDecoder(getRecordDecoder())],
    ["resolver", getOptionDecoder(getAddressDecoder())],
    [
      "hostingRef",
      getOptionDecoder(addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())),
    ],
    ["transferLocked", getBooleanDecoder()],
    ["reverseSet", getBooleanDecoder()],
    ["nftMint", getOptionDecoder(getAddressDecoder())],
    ["parent", getOptionDecoder(getAddressDecoder())],
    ["parentRegisteredAt", getI64Decoder()],
    ["depth", getU8Decoder()],
    ["listed", getBooleanDecoder()],
    ["bump", getU8Decoder()],
  ]);
}

/** Gets the codec for {@link NameRecord} account data. */
export function getNameRecordCodec(): Codec<NameRecordArgs, NameRecord> {
  return combineCodec(getNameRecordEncoder(), getNameRecordDecoder());
}

export function decodeNameRecord<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>,
): Account<NameRecord, TAddress>;
export function decodeNameRecord<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>,
): MaybeAccount<NameRecord, TAddress>;
export function decodeNameRecord<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>,
): Account<NameRecord, TAddress> | MaybeAccount<NameRecord, TAddress> {
  return decodeAccount(
    encodedAccount as MaybeEncodedAccount<TAddress>,
    getNameRecordDecoder(),
  );
}

export async function fetchNameRecord<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<Account<NameRecord, TAddress>> {
  const maybeAccount = await fetchMaybeNameRecord(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeNameRecord<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<MaybeAccount<NameRecord, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeNameRecord(maybeAccount);
}

export async function fetchAllNameRecord(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<Account<NameRecord>[]> {
  const maybeAccounts = await fetchAllMaybeNameRecord(rpc, addresses, config);
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeNameRecord(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<MaybeAccount<NameRecord>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) => decodeNameRecord(maybeAccount));
}
