/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  addDecoderSizePrefix,
  addEncoderSizePrefix,
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getU8Decoder,
  getU8Encoder,
  getUtf8Decoder,
  getUtf8Encoder,
  transformEncoder,
  type Account,
  type Address,
  type Codec,
  type Decoder,
  type EncodedAccount,
  type Encoder,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type MaybeAccount,
  type MaybeEncodedAccount,
  type ReadonlyUint8Array,
} from "@solana/kit";

export const REVERSE_RECORD_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  125, 98, 59, 20, 159, 18, 85, 123,
]);

export function getReverseRecordDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    REVERSE_RECORD_DISCRIMINATOR,
  );
}

export type ReverseRecord = {
  discriminator: ReadonlyUint8Array;
  /** The wallet this reverse record belongs to. */
  owner: Address;
  /** keccak256 hash of the name it points to (matches `NameRecord.name_hash`). */
  nameHash: ReadonlyUint8Array;
  /** Human-readable name label (e.g. "alex"). */
  name: string;
  /** Top-level domain (e.g. "sol"). */
  tld: string;
  /** Canonical PDA bump. */
  bump: number;
};

export type ReverseRecordArgs = {
  /** The wallet this reverse record belongs to. */
  owner: Address;
  /** keccak256 hash of the name it points to (matches `NameRecord.name_hash`). */
  nameHash: ReadonlyUint8Array;
  /** Human-readable name label (e.g. "alex"). */
  name: string;
  /** Top-level domain (e.g. "sol"). */
  tld: string;
  /** Canonical PDA bump. */
  bump: number;
};

/** Gets the encoder for {@link ReverseRecordArgs} account data. */
export function getReverseRecordEncoder(): Encoder<ReverseRecordArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["owner", getAddressEncoder()],
      ["nameHash", fixEncoderSize(getBytesEncoder(), 32)],
      ["name", addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
      ["tld", addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
      ["bump", getU8Encoder()],
    ]),
    (value) => ({ ...value, discriminator: REVERSE_RECORD_DISCRIMINATOR }),
  );
}

/** Gets the decoder for {@link ReverseRecord} account data. */
export function getReverseRecordDecoder(): Decoder<ReverseRecord> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["owner", getAddressDecoder()],
    ["nameHash", fixDecoderSize(getBytesDecoder(), 32)],
    ["name", addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
    ["tld", addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
    ["bump", getU8Decoder()],
  ]);
}

/** Gets the codec for {@link ReverseRecord} account data. */
export function getReverseRecordCodec(): Codec<
  ReverseRecordArgs,
  ReverseRecord
> {
  return combineCodec(getReverseRecordEncoder(), getReverseRecordDecoder());
}

export function decodeReverseRecord<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>,
): Account<ReverseRecord, TAddress>;
export function decodeReverseRecord<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>,
): MaybeAccount<ReverseRecord, TAddress>;
export function decodeReverseRecord<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>,
): Account<ReverseRecord, TAddress> | MaybeAccount<ReverseRecord, TAddress> {
  return decodeAccount(
    encodedAccount as MaybeEncodedAccount<TAddress>,
    getReverseRecordDecoder(),
  );
}

export async function fetchReverseRecord<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<Account<ReverseRecord, TAddress>> {
  const maybeAccount = await fetchMaybeReverseRecord(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeReverseRecord<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<MaybeAccount<ReverseRecord, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeReverseRecord(maybeAccount);
}

export async function fetchAllReverseRecord(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<Account<ReverseRecord>[]> {
  const maybeAccounts = await fetchAllMaybeReverseRecord(
    rpc,
    addresses,
    config,
  );
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeReverseRecord(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<MaybeAccount<ReverseRecord>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) => decodeReverseRecord(maybeAccount));
}
