/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/kit/program-client-core";
import { SOLANS_PROGRAM_ADDRESS } from "../programs";

export const BID_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  199, 56, 85, 38, 146, 243, 37, 158,
]);

export function getBidDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(BID_DISCRIMINATOR);
}

export type BidInstruction<
  TProgram extends string = typeof SOLANS_PROGRAM_ADDRESS,
  TAccountBidder extends string | AccountMeta<string> = string,
  TAccountAuction extends string | AccountMeta<string> = string,
  TAccountBidderSolansAccount extends string | AccountMeta<string> = string,
  TAccountPrevBidderSolansAccount extends string | AccountMeta<string> = string,
  TAccountBidVault extends string | AccountMeta<string> = string,
  TAccountSolansMint extends string | AccountMeta<string> = string,
  TAccountTokenProgram extends string | AccountMeta<string> =
    "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountBidder extends string
        ? WritableSignerAccount<TAccountBidder> &
            AccountSignerMeta<TAccountBidder>
        : TAccountBidder,
      TAccountAuction extends string
        ? WritableAccount<TAccountAuction>
        : TAccountAuction,
      TAccountBidderSolansAccount extends string
        ? WritableAccount<TAccountBidderSolansAccount>
        : TAccountBidderSolansAccount,
      TAccountPrevBidderSolansAccount extends string
        ? WritableAccount<TAccountPrevBidderSolansAccount>
        : TAccountPrevBidderSolansAccount,
      TAccountBidVault extends string
        ? WritableAccount<TAccountBidVault>
        : TAccountBidVault,
      TAccountSolansMint extends string
        ? ReadonlyAccount<TAccountSolansMint>
        : TAccountSolansMint,
      TAccountTokenProgram extends string
        ? ReadonlyAccount<TAccountTokenProgram>
        : TAccountTokenProgram,
      ...TRemainingAccounts,
    ]
  >;

export type BidInstructionData = {
  discriminator: ReadonlyUint8Array;
  amount: bigint;
};

export type BidInstructionDataArgs = { amount: number | bigint };

export function getBidInstructionDataEncoder(): FixedSizeEncoder<BidInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["amount", getU64Encoder()],
    ]),
    (value) => ({ ...value, discriminator: BID_DISCRIMINATOR }),
  );
}

export function getBidInstructionDataDecoder(): FixedSizeDecoder<BidInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["amount", getU64Decoder()],
  ]);
}

export function getBidInstructionDataCodec(): FixedSizeCodec<
  BidInstructionDataArgs,
  BidInstructionData
> {
  return combineCodec(
    getBidInstructionDataEncoder(),
    getBidInstructionDataDecoder(),
  );
}

export type BidInput<
  TAccountBidder extends string = string,
  TAccountAuction extends string = string,
  TAccountBidderSolansAccount extends string = string,
  TAccountPrevBidderSolansAccount extends string = string,
  TAccountBidVault extends string = string,
  TAccountSolansMint extends string = string,
  TAccountTokenProgram extends string = string,
> = {
  bidder: TransactionSigner<TAccountBidder>;
  auction: Address<TAccountAuction>;
  /** Bidder's `$SOLANS` account; the bid is pulled from here. */
  bidderSolansAccount: Address<TAccountBidderSolansAccount>;
  /**
   * The current highest bidder's `$SOLANS` account; required when there is a
   * standing bid so the outbid refund lands. Must belong to `highest_bidder`.
   */
  prevBidderSolansAccount?: Address<TAccountPrevBidderSolansAccount>;
  /** Auction-owned escrow; pinned to `auction.bid_vault`, which fixes `solans_mint`. */
  bidVault: Address<TAccountBidVault>;
  solansMint: Address<TAccountSolansMint>;
  tokenProgram?: Address<TAccountTokenProgram>;
  amount: BidInstructionDataArgs["amount"];
};

export function getBidInstruction<
  TAccountBidder extends string,
  TAccountAuction extends string,
  TAccountBidderSolansAccount extends string,
  TAccountPrevBidderSolansAccount extends string,
  TAccountBidVault extends string,
  TAccountSolansMint extends string,
  TAccountTokenProgram extends string,
  TProgramAddress extends Address = typeof SOLANS_PROGRAM_ADDRESS,
>(
  input: BidInput<
    TAccountBidder,
    TAccountAuction,
    TAccountBidderSolansAccount,
    TAccountPrevBidderSolansAccount,
    TAccountBidVault,
    TAccountSolansMint,
    TAccountTokenProgram
  >,
  config?: { programAddress?: TProgramAddress },
): BidInstruction<
  TProgramAddress,
  TAccountBidder,
  TAccountAuction,
  TAccountBidderSolansAccount,
  TAccountPrevBidderSolansAccount,
  TAccountBidVault,
  TAccountSolansMint,
  TAccountTokenProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? SOLANS_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    bidder: { value: input.bidder ?? null, isWritable: true },
    auction: { value: input.auction ?? null, isWritable: true },
    bidderSolansAccount: {
      value: input.bidderSolansAccount ?? null,
      isWritable: true,
    },
    prevBidderSolansAccount: {
      value: input.prevBidderSolansAccount ?? null,
      isWritable: true,
    },
    bidVault: { value: input.bidVault ?? null, isWritable: true },
    solansMint: { value: input.solansMint ?? null, isWritable: false },
    tokenProgram: { value: input.tokenProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.tokenProgram.value) {
    accounts.tokenProgram.value =
      "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA" as Address<"TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("bidder", accounts.bidder),
      getAccountMeta("auction", accounts.auction),
      getAccountMeta("bidderSolansAccount", accounts.bidderSolansAccount),
      getAccountMeta(
        "prevBidderSolansAccount",
        accounts.prevBidderSolansAccount,
      ),
      getAccountMeta("bidVault", accounts.bidVault),
      getAccountMeta("solansMint", accounts.solansMint),
      getAccountMeta("tokenProgram", accounts.tokenProgram),
    ],
    data: getBidInstructionDataEncoder().encode(args as BidInstructionDataArgs),
    programAddress,
  } as BidInstruction<
    TProgramAddress,
    TAccountBidder,
    TAccountAuction,
    TAccountBidderSolansAccount,
    TAccountPrevBidderSolansAccount,
    TAccountBidVault,
    TAccountSolansMint,
    TAccountTokenProgram
  >);
}

export type ParsedBidInstruction<
  TProgram extends string = typeof SOLANS_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    bidder: TAccountMetas[0];
    auction: TAccountMetas[1];
    /** Bidder's `$SOLANS` account; the bid is pulled from here. */
    bidderSolansAccount: TAccountMetas[2];
    /**
     * The current highest bidder's `$SOLANS` account; required when there is a
     * standing bid so the outbid refund lands. Must belong to `highest_bidder`.
     */
    prevBidderSolansAccount?: TAccountMetas[3] | undefined;
    /** Auction-owned escrow; pinned to `auction.bid_vault`, which fixes `solans_mint`. */
    bidVault: TAccountMetas[4];
    solansMint: TAccountMetas[5];
    tokenProgram: TAccountMetas[6];
  };
  data: BidInstructionData;
};

export function parseBidInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedBidInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 7) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 7,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  const getNextOptionalAccount = () => {
    const accountMeta = getNextAccount();
    return accountMeta.address === SOLANS_PROGRAM_ADDRESS
      ? undefined
      : accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      bidder: getNextAccount(),
      auction: getNextAccount(),
      bidderSolansAccount: getNextAccount(),
      prevBidderSolansAccount: getNextOptionalAccount(),
      bidVault: getNextAccount(),
      solansMint: getNextAccount(),
      tokenProgram: getNextAccount(),
    },
    data: getBidInstructionDataDecoder().decode(instruction.data),
  };
}
