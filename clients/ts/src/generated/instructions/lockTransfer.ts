/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBooleanDecoder,
  getBooleanEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/kit/program-client-core";
import { SOLANS_PROGRAM_ADDRESS } from "../programs";

export const LOCK_TRANSFER_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  141, 30, 149, 158, 41, 243, 55, 225,
]);

export function getLockTransferDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    LOCK_TRANSFER_DISCRIMINATOR,
  );
}

export type LockTransferInstruction<
  TProgram extends string = typeof SOLANS_PROGRAM_ADDRESS,
  TAccountOwner extends string | AccountMeta<string> = string,
  TAccountNameRecord extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountOwner extends string
        ? ReadonlySignerAccount<TAccountOwner> &
            AccountSignerMeta<TAccountOwner>
        : TAccountOwner,
      TAccountNameRecord extends string
        ? WritableAccount<TAccountNameRecord>
        : TAccountNameRecord,
      ...TRemainingAccounts,
    ]
  >;

export type LockTransferInstructionData = {
  discriminator: ReadonlyUint8Array;
  lock: boolean;
};

export type LockTransferInstructionDataArgs = { lock: boolean };

export function getLockTransferInstructionDataEncoder(): FixedSizeEncoder<LockTransferInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["lock", getBooleanEncoder()],
    ]),
    (value) => ({ ...value, discriminator: LOCK_TRANSFER_DISCRIMINATOR }),
  );
}

export function getLockTransferInstructionDataDecoder(): FixedSizeDecoder<LockTransferInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["lock", getBooleanDecoder()],
  ]);
}

export function getLockTransferInstructionDataCodec(): FixedSizeCodec<
  LockTransferInstructionDataArgs,
  LockTransferInstructionData
> {
  return combineCodec(
    getLockTransferInstructionDataEncoder(),
    getLockTransferInstructionDataDecoder(),
  );
}

export type LockTransferInput<
  TAccountOwner extends string = string,
  TAccountNameRecord extends string = string,
> = {
  owner: TransactionSigner<TAccountOwner>;
  nameRecord: Address<TAccountNameRecord>;
  lock: LockTransferInstructionDataArgs["lock"];
};

export function getLockTransferInstruction<
  TAccountOwner extends string,
  TAccountNameRecord extends string,
  TProgramAddress extends Address = typeof SOLANS_PROGRAM_ADDRESS,
>(
  input: LockTransferInput<TAccountOwner, TAccountNameRecord>,
  config?: { programAddress?: TProgramAddress },
): LockTransferInstruction<TProgramAddress, TAccountOwner, TAccountNameRecord> {
  // Program address.
  const programAddress = config?.programAddress ?? SOLANS_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    owner: { value: input.owner ?? null, isWritable: false },
    nameRecord: { value: input.nameRecord ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("owner", accounts.owner),
      getAccountMeta("nameRecord", accounts.nameRecord),
    ],
    data: getLockTransferInstructionDataEncoder().encode(
      args as LockTransferInstructionDataArgs,
    ),
    programAddress,
  } as LockTransferInstruction<
    TProgramAddress,
    TAccountOwner,
    TAccountNameRecord
  >);
}

export type ParsedLockTransferInstruction<
  TProgram extends string = typeof SOLANS_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    owner: TAccountMetas[0];
    nameRecord: TAccountMetas[1];
  };
  data: LockTransferInstructionData;
};

export function parseLockTransferInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedLockTransferInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 2,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { owner: getNextAccount(), nameRecord: getNextAccount() },
    data: getLockTransferInstructionDataDecoder().decode(instruction.data),
  };
}
