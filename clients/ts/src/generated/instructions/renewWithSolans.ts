/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  addDecoderSizePrefix,
  addEncoderSizePrefix,
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU16Decoder,
  getU16Encoder,
  getU32Decoder,
  getU32Encoder,
  getUtf8Decoder,
  getUtf8Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/kit/program-client-core";
import { findConfigPda } from "../pdas";
import { SOLANS_PROGRAM_ADDRESS } from "../programs";

export const RENEW_WITH_SOLANS_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([216, 153, 16, 43, 181, 31, 35, 80]);

export function getRenewWithSolansDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    RENEW_WITH_SOLANS_DISCRIMINATOR,
  );
}

export type RenewWithSolansInstruction<
  TProgram extends string = typeof SOLANS_PROGRAM_ADDRESS,
  TAccountPayer extends string | AccountMeta<string> = string,
  TAccountConfig extends string | AccountMeta<string> = string,
  TAccountNameRecord extends string | AccountMeta<string> = string,
  TAccountPayerSolansAccount extends string | AccountMeta<string> = string,
  TAccountSolansMint extends string | AccountMeta<string> = string,
  TAccountTokenProgram extends string | AccountMeta<string> =
    "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountPayer extends string
        ? WritableSignerAccount<TAccountPayer> &
            AccountSignerMeta<TAccountPayer>
        : TAccountPayer,
      TAccountConfig extends string
        ? ReadonlyAccount<TAccountConfig>
        : TAccountConfig,
      TAccountNameRecord extends string
        ? WritableAccount<TAccountNameRecord>
        : TAccountNameRecord,
      TAccountPayerSolansAccount extends string
        ? WritableAccount<TAccountPayerSolansAccount>
        : TAccountPayerSolansAccount,
      TAccountSolansMint extends string
        ? WritableAccount<TAccountSolansMint>
        : TAccountSolansMint,
      TAccountTokenProgram extends string
        ? ReadonlyAccount<TAccountTokenProgram>
        : TAccountTokenProgram,
      ...TRemainingAccounts,
    ]
  >;

export type RenewWithSolansInstructionData = {
  discriminator: ReadonlyUint8Array;
  name: string;
  tld: string;
  years: number;
};

export type RenewWithSolansInstructionDataArgs = {
  name: string;
  tld: string;
  years: number;
};

export function getRenewWithSolansInstructionDataEncoder(): Encoder<RenewWithSolansInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["name", addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
      ["tld", addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
      ["years", getU16Encoder()],
    ]),
    (value) => ({ ...value, discriminator: RENEW_WITH_SOLANS_DISCRIMINATOR }),
  );
}

export function getRenewWithSolansInstructionDataDecoder(): Decoder<RenewWithSolansInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["name", addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
    ["tld", addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
    ["years", getU16Decoder()],
  ]);
}

export function getRenewWithSolansInstructionDataCodec(): Codec<
  RenewWithSolansInstructionDataArgs,
  RenewWithSolansInstructionData
> {
  return combineCodec(
    getRenewWithSolansInstructionDataEncoder(),
    getRenewWithSolansInstructionDataDecoder(),
  );
}

export type RenewWithSolansAsyncInput<
  TAccountPayer extends string = string,
  TAccountConfig extends string = string,
  TAccountNameRecord extends string = string,
  TAccountPayerSolansAccount extends string = string,
  TAccountSolansMint extends string = string,
  TAccountTokenProgram extends string = string,
> = {
  payer: TransactionSigner<TAccountPayer>;
  config?: Address<TAccountConfig>;
  nameRecord: Address<TAccountNameRecord>;
  /** Payer's `$SOLANS` account; the discounted fee is burned from here. */
  payerSolansAccount: Address<TAccountPayerSolansAccount>;
  solansMint: Address<TAccountSolansMint>;
  tokenProgram?: Address<TAccountTokenProgram>;
  name: RenewWithSolansInstructionDataArgs["name"];
  tld: RenewWithSolansInstructionDataArgs["tld"];
  years: RenewWithSolansInstructionDataArgs["years"];
};

export async function getRenewWithSolansInstructionAsync<
  TAccountPayer extends string,
  TAccountConfig extends string,
  TAccountNameRecord extends string,
  TAccountPayerSolansAccount extends string,
  TAccountSolansMint extends string,
  TAccountTokenProgram extends string,
  TProgramAddress extends Address = typeof SOLANS_PROGRAM_ADDRESS,
>(
  input: RenewWithSolansAsyncInput<
    TAccountPayer,
    TAccountConfig,
    TAccountNameRecord,
    TAccountPayerSolansAccount,
    TAccountSolansMint,
    TAccountTokenProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  RenewWithSolansInstruction<
    TProgramAddress,
    TAccountPayer,
    TAccountConfig,
    TAccountNameRecord,
    TAccountPayerSolansAccount,
    TAccountSolansMint,
    TAccountTokenProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? SOLANS_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    payer: { value: input.payer ?? null, isWritable: true },
    config: { value: input.config ?? null, isWritable: false },
    nameRecord: { value: input.nameRecord ?? null, isWritable: true },
    payerSolansAccount: {
      value: input.payerSolansAccount ?? null,
      isWritable: true,
    },
    solansMint: { value: input.solansMint ?? null, isWritable: true },
    tokenProgram: { value: input.tokenProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.config.value) {
    accounts.config.value = await findConfigPda();
  }
  if (!accounts.tokenProgram.value) {
    accounts.tokenProgram.value =
      "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA" as Address<"TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("payer", accounts.payer),
      getAccountMeta("config", accounts.config),
      getAccountMeta("nameRecord", accounts.nameRecord),
      getAccountMeta("payerSolansAccount", accounts.payerSolansAccount),
      getAccountMeta("solansMint", accounts.solansMint),
      getAccountMeta("tokenProgram", accounts.tokenProgram),
    ],
    data: getRenewWithSolansInstructionDataEncoder().encode(
      args as RenewWithSolansInstructionDataArgs,
    ),
    programAddress,
  } as RenewWithSolansInstruction<
    TProgramAddress,
    TAccountPayer,
    TAccountConfig,
    TAccountNameRecord,
    TAccountPayerSolansAccount,
    TAccountSolansMint,
    TAccountTokenProgram
  >);
}

export type RenewWithSolansInput<
  TAccountPayer extends string = string,
  TAccountConfig extends string = string,
  TAccountNameRecord extends string = string,
  TAccountPayerSolansAccount extends string = string,
  TAccountSolansMint extends string = string,
  TAccountTokenProgram extends string = string,
> = {
  payer: TransactionSigner<TAccountPayer>;
  config: Address<TAccountConfig>;
  nameRecord: Address<TAccountNameRecord>;
  /** Payer's `$SOLANS` account; the discounted fee is burned from here. */
  payerSolansAccount: Address<TAccountPayerSolansAccount>;
  solansMint: Address<TAccountSolansMint>;
  tokenProgram?: Address<TAccountTokenProgram>;
  name: RenewWithSolansInstructionDataArgs["name"];
  tld: RenewWithSolansInstructionDataArgs["tld"];
  years: RenewWithSolansInstructionDataArgs["years"];
};

export function getRenewWithSolansInstruction<
  TAccountPayer extends string,
  TAccountConfig extends string,
  TAccountNameRecord extends string,
  TAccountPayerSolansAccount extends string,
  TAccountSolansMint extends string,
  TAccountTokenProgram extends string,
  TProgramAddress extends Address = typeof SOLANS_PROGRAM_ADDRESS,
>(
  input: RenewWithSolansInput<
    TAccountPayer,
    TAccountConfig,
    TAccountNameRecord,
    TAccountPayerSolansAccount,
    TAccountSolansMint,
    TAccountTokenProgram
  >,
  config?: { programAddress?: TProgramAddress },
): RenewWithSolansInstruction<
  TProgramAddress,
  TAccountPayer,
  TAccountConfig,
  TAccountNameRecord,
  TAccountPayerSolansAccount,
  TAccountSolansMint,
  TAccountTokenProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? SOLANS_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    payer: { value: input.payer ?? null, isWritable: true },
    config: { value: input.config ?? null, isWritable: false },
    nameRecord: { value: input.nameRecord ?? null, isWritable: true },
    payerSolansAccount: {
      value: input.payerSolansAccount ?? null,
      isWritable: true,
    },
    solansMint: { value: input.solansMint ?? null, isWritable: true },
    tokenProgram: { value: input.tokenProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.tokenProgram.value) {
    accounts.tokenProgram.value =
      "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA" as Address<"TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("payer", accounts.payer),
      getAccountMeta("config", accounts.config),
      getAccountMeta("nameRecord", accounts.nameRecord),
      getAccountMeta("payerSolansAccount", accounts.payerSolansAccount),
      getAccountMeta("solansMint", accounts.solansMint),
      getAccountMeta("tokenProgram", accounts.tokenProgram),
    ],
    data: getRenewWithSolansInstructionDataEncoder().encode(
      args as RenewWithSolansInstructionDataArgs,
    ),
    programAddress,
  } as RenewWithSolansInstruction<
    TProgramAddress,
    TAccountPayer,
    TAccountConfig,
    TAccountNameRecord,
    TAccountPayerSolansAccount,
    TAccountSolansMint,
    TAccountTokenProgram
  >);
}

export type ParsedRenewWithSolansInstruction<
  TProgram extends string = typeof SOLANS_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    payer: TAccountMetas[0];
    config: TAccountMetas[1];
    nameRecord: TAccountMetas[2];
    /** Payer's `$SOLANS` account; the discounted fee is burned from here. */
    payerSolansAccount: TAccountMetas[3];
    solansMint: TAccountMetas[4];
    tokenProgram: TAccountMetas[5];
  };
  data: RenewWithSolansInstructionData;
};

export function parseRenewWithSolansInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedRenewWithSolansInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 6) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 6,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      payer: getNextAccount(),
      config: getNextAccount(),
      nameRecord: getNextAccount(),
      payerSolansAccount: getNextAccount(),
      solansMint: getNextAccount(),
      tokenProgram: getNextAccount(),
    },
    data: getRenewWithSolansInstructionDataDecoder().decode(instruction.data),
  };
}
