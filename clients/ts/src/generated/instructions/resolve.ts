/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  addDecoderSizePrefix,
  addEncoderSizePrefix,
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getOptionDecoder,
  getOptionEncoder,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getUtf8Decoder,
  getUtf8Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type Option,
  type OptionOrNullable,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/kit/program-client-core";
import { SOLANS_PROGRAM_ADDRESS } from "../programs";

export const RESOLVE_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  246, 150, 236, 206, 108, 63, 58, 10,
]);

export function getResolveDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(RESOLVE_DISCRIMINATOR);
}

export type ResolveInstruction<
  TProgram extends string = typeof SOLANS_PROGRAM_ADDRESS,
  TAccountNameRecord extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountNameRecord extends string
        ? ReadonlyAccount<TAccountNameRecord>
        : TAccountNameRecord,
      ...TRemainingAccounts,
    ]
  >;

export type ResolveInstructionData = {
  discriminator: ReadonlyUint8Array;
  nameHash: ReadonlyUint8Array;
  recordKey: Option<string>;
};

export type ResolveInstructionDataArgs = {
  nameHash: ReadonlyUint8Array;
  recordKey: OptionOrNullable<string>;
};

export function getResolveInstructionDataEncoder(): Encoder<ResolveInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["nameHash", fixEncoderSize(getBytesEncoder(), 32)],
      [
        "recordKey",
        getOptionEncoder(
          addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder()),
        ),
      ],
    ]),
    (value) => ({ ...value, discriminator: RESOLVE_DISCRIMINATOR }),
  );
}

export function getResolveInstructionDataDecoder(): Decoder<ResolveInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["nameHash", fixDecoderSize(getBytesDecoder(), 32)],
    [
      "recordKey",
      getOptionDecoder(addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())),
    ],
  ]);
}

export function getResolveInstructionDataCodec(): Codec<
  ResolveInstructionDataArgs,
  ResolveInstructionData
> {
  return combineCodec(
    getResolveInstructionDataEncoder(),
    getResolveInstructionDataDecoder(),
  );
}

export type ResolveInput<TAccountNameRecord extends string = string> = {
  nameRecord: Address<TAccountNameRecord>;
  nameHash: ResolveInstructionDataArgs["nameHash"];
  recordKey: ResolveInstructionDataArgs["recordKey"];
};

export function getResolveInstruction<
  TAccountNameRecord extends string,
  TProgramAddress extends Address = typeof SOLANS_PROGRAM_ADDRESS,
>(
  input: ResolveInput<TAccountNameRecord>,
  config?: { programAddress?: TProgramAddress },
): ResolveInstruction<TProgramAddress, TAccountNameRecord> {
  // Program address.
  const programAddress = config?.programAddress ?? SOLANS_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    nameRecord: { value: input.nameRecord ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [getAccountMeta("nameRecord", accounts.nameRecord)],
    data: getResolveInstructionDataEncoder().encode(
      args as ResolveInstructionDataArgs,
    ),
    programAddress,
  } as ResolveInstruction<TProgramAddress, TAccountNameRecord>);
}

export type ParsedResolveInstruction<
  TProgram extends string = typeof SOLANS_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    nameRecord: TAccountMetas[0];
  };
  data: ResolveInstructionData;
};

export function parseResolveInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedResolveInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 1) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 1,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { nameRecord: getNextAccount() },
    data: getResolveInstructionDataDecoder().decode(instruction.data),
  };
}
