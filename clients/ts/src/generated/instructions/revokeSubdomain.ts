/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/kit/program-client-core";
import { SOLANS_PROGRAM_ADDRESS } from "../programs";

export const REVOKE_SUBDOMAIN_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([161, 15, 96, 214, 145, 88, 83, 118]);

export function getRevokeSubdomainDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    REVOKE_SUBDOMAIN_DISCRIMINATOR,
  );
}

export type RevokeSubdomainInstruction<
  TProgram extends string = typeof SOLANS_PROGRAM_ADDRESS,
  TAccountOwner extends string | AccountMeta<string> = string,
  TAccountParentName extends string | AccountMeta<string> = string,
  TAccountNameRecord extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountOwner extends string
        ? WritableSignerAccount<TAccountOwner> &
            AccountSignerMeta<TAccountOwner>
        : TAccountOwner,
      TAccountParentName extends string
        ? ReadonlyAccount<TAccountParentName>
        : TAccountParentName,
      TAccountNameRecord extends string
        ? WritableAccount<TAccountNameRecord>
        : TAccountNameRecord,
      ...TRemainingAccounts,
    ]
  >;

export type RevokeSubdomainInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type RevokeSubdomainInstructionDataArgs = {};

export function getRevokeSubdomainInstructionDataEncoder(): FixedSizeEncoder<RevokeSubdomainInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: REVOKE_SUBDOMAIN_DISCRIMINATOR }),
  );
}

export function getRevokeSubdomainInstructionDataDecoder(): FixedSizeDecoder<RevokeSubdomainInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getRevokeSubdomainInstructionDataCodec(): FixedSizeCodec<
  RevokeSubdomainInstructionDataArgs,
  RevokeSubdomainInstructionData
> {
  return combineCodec(
    getRevokeSubdomainInstructionDataEncoder(),
    getRevokeSubdomainInstructionDataDecoder(),
  );
}

export type RevokeSubdomainInput<
  TAccountOwner extends string = string,
  TAccountParentName extends string = string,
  TAccountNameRecord extends string = string,
> = {
  owner: TransactionSigner<TAccountOwner>;
  parentName: Address<TAccountParentName>;
  nameRecord: Address<TAccountNameRecord>;
};

export function getRevokeSubdomainInstruction<
  TAccountOwner extends string,
  TAccountParentName extends string,
  TAccountNameRecord extends string,
  TProgramAddress extends Address = typeof SOLANS_PROGRAM_ADDRESS,
>(
  input: RevokeSubdomainInput<
    TAccountOwner,
    TAccountParentName,
    TAccountNameRecord
  >,
  config?: { programAddress?: TProgramAddress },
): RevokeSubdomainInstruction<
  TProgramAddress,
  TAccountOwner,
  TAccountParentName,
  TAccountNameRecord
> {
  // Program address.
  const programAddress = config?.programAddress ?? SOLANS_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    owner: { value: input.owner ?? null, isWritable: true },
    parentName: { value: input.parentName ?? null, isWritable: false },
    nameRecord: { value: input.nameRecord ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("owner", accounts.owner),
      getAccountMeta("parentName", accounts.parentName),
      getAccountMeta("nameRecord", accounts.nameRecord),
    ],
    data: getRevokeSubdomainInstructionDataEncoder().encode({}),
    programAddress,
  } as RevokeSubdomainInstruction<
    TProgramAddress,
    TAccountOwner,
    TAccountParentName,
    TAccountNameRecord
  >);
}

export type ParsedRevokeSubdomainInstruction<
  TProgram extends string = typeof SOLANS_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    owner: TAccountMetas[0];
    parentName: TAccountMetas[1];
    nameRecord: TAccountMetas[2];
  };
  data: RevokeSubdomainInstructionData;
};

export function parseRevokeSubdomainInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedRevokeSubdomainInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 3,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      owner: getNextAccount(),
      parentName: getNextAccount(),
      nameRecord: getNextAccount(),
    },
    data: getRevokeSubdomainInstructionDataDecoder().decode(instruction.data),
  };
}
