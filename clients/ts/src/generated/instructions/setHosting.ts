/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  addDecoderSizePrefix,
  addEncoderSizePrefix,
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getOptionDecoder,
  getOptionEncoder,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getUtf8Decoder,
  getUtf8Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type Option,
  type OptionOrNullable,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/kit/program-client-core";
import { SOLANS_PROGRAM_ADDRESS } from "../programs";

export const SET_HOSTING_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  213, 38, 154, 154, 19, 200, 116, 114,
]);

export function getSetHostingDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(SET_HOSTING_DISCRIMINATOR);
}

export type SetHostingInstruction<
  TProgram extends string = typeof SOLANS_PROGRAM_ADDRESS,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountNameRecord extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAuthority extends string
        ? ReadonlySignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountNameRecord extends string
        ? WritableAccount<TAccountNameRecord>
        : TAccountNameRecord,
      ...TRemainingAccounts,
    ]
  >;

export type SetHostingInstructionData = {
  discriminator: ReadonlyUint8Array;
  hostingRef: Option<string>;
};

export type SetHostingInstructionDataArgs = {
  hostingRef: OptionOrNullable<string>;
};

export function getSetHostingInstructionDataEncoder(): Encoder<SetHostingInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      [
        "hostingRef",
        getOptionEncoder(
          addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder()),
        ),
      ],
    ]),
    (value) => ({ ...value, discriminator: SET_HOSTING_DISCRIMINATOR }),
  );
}

export function getSetHostingInstructionDataDecoder(): Decoder<SetHostingInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    [
      "hostingRef",
      getOptionDecoder(addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())),
    ],
  ]);
}

export function getSetHostingInstructionDataCodec(): Codec<
  SetHostingInstructionDataArgs,
  SetHostingInstructionData
> {
  return combineCodec(
    getSetHostingInstructionDataEncoder(),
    getSetHostingInstructionDataDecoder(),
  );
}

export type SetHostingInput<
  TAccountAuthority extends string = string,
  TAccountNameRecord extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  nameRecord: Address<TAccountNameRecord>;
  hostingRef: SetHostingInstructionDataArgs["hostingRef"];
};

export function getSetHostingInstruction<
  TAccountAuthority extends string,
  TAccountNameRecord extends string,
  TProgramAddress extends Address = typeof SOLANS_PROGRAM_ADDRESS,
>(
  input: SetHostingInput<TAccountAuthority, TAccountNameRecord>,
  config?: { programAddress?: TProgramAddress },
): SetHostingInstruction<
  TProgramAddress,
  TAccountAuthority,
  TAccountNameRecord
> {
  // Program address.
  const programAddress = config?.programAddress ?? SOLANS_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: false },
    nameRecord: { value: input.nameRecord ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("authority", accounts.authority),
      getAccountMeta("nameRecord", accounts.nameRecord),
    ],
    data: getSetHostingInstructionDataEncoder().encode(
      args as SetHostingInstructionDataArgs,
    ),
    programAddress,
  } as SetHostingInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountNameRecord
  >);
}

export type ParsedSetHostingInstruction<
  TProgram extends string = typeof SOLANS_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    authority: TAccountMetas[0];
    nameRecord: TAccountMetas[1];
  };
  data: SetHostingInstructionData;
};

export function parseSetHostingInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedSetHostingInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 2,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { authority: getNextAccount(), nameRecord: getNextAccount() },
    data: getSetHostingInstructionDataDecoder().decode(instruction.data),
  };
}
