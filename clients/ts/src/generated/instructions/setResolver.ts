/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getOptionDecoder,
  getOptionEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type Option,
  type OptionOrNullable,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/kit/program-client-core";
import { SOLANS_PROGRAM_ADDRESS } from "../programs";

export const SET_RESOLVER_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  137, 108, 27, 51, 202, 16, 33, 119,
]);

export function getSetResolverDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    SET_RESOLVER_DISCRIMINATOR,
  );
}

export type SetResolverInstruction<
  TProgram extends string = typeof SOLANS_PROGRAM_ADDRESS,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountNameRecord extends string | AccountMeta<string> = string,
  TAccountNftTokenAccount extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAuthority extends string
        ? ReadonlySignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountNameRecord extends string
        ? WritableAccount<TAccountNameRecord>
        : TAccountNameRecord,
      TAccountNftTokenAccount extends string
        ? ReadonlyAccount<TAccountNftTokenAccount>
        : TAccountNftTokenAccount,
      ...TRemainingAccounts,
    ]
  >;

export type SetResolverInstructionData = {
  discriminator: ReadonlyUint8Array;
  resolver: Option<Address>;
};

export type SetResolverInstructionDataArgs = {
  resolver: OptionOrNullable<Address>;
};

export function getSetResolverInstructionDataEncoder(): Encoder<SetResolverInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["resolver", getOptionEncoder(getAddressEncoder())],
    ]),
    (value) => ({ ...value, discriminator: SET_RESOLVER_DISCRIMINATOR }),
  );
}

export function getSetResolverInstructionDataDecoder(): Decoder<SetResolverInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["resolver", getOptionDecoder(getAddressDecoder())],
  ]);
}

export function getSetResolverInstructionDataCodec(): Codec<
  SetResolverInstructionDataArgs,
  SetResolverInstructionData
> {
  return combineCodec(
    getSetResolverInstructionDataEncoder(),
    getSetResolverInstructionDataDecoder(),
  );
}

export type SetResolverInput<
  TAccountAuthority extends string = string,
  TAccountNameRecord extends string = string,
  TAccountNftTokenAccount extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  nameRecord: Address<TAccountNameRecord>;
  /**
   * Proof of NFT holdership, required only while the name is tokenized: the
   * holder (not the stale `owner`) manages the resolver attribute — a dynamic
   * NFT (spec §6.1). Omitted (`None`) for non-tokenized names.
   */
  nftTokenAccount?: Address<TAccountNftTokenAccount>;
  resolver: SetResolverInstructionDataArgs["resolver"];
};

export function getSetResolverInstruction<
  TAccountAuthority extends string,
  TAccountNameRecord extends string,
  TAccountNftTokenAccount extends string,
  TProgramAddress extends Address = typeof SOLANS_PROGRAM_ADDRESS,
>(
  input: SetResolverInput<
    TAccountAuthority,
    TAccountNameRecord,
    TAccountNftTokenAccount
  >,
  config?: { programAddress?: TProgramAddress },
): SetResolverInstruction<
  TProgramAddress,
  TAccountAuthority,
  TAccountNameRecord,
  TAccountNftTokenAccount
> {
  // Program address.
  const programAddress = config?.programAddress ?? SOLANS_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: false },
    nameRecord: { value: input.nameRecord ?? null, isWritable: true },
    nftTokenAccount: {
      value: input.nftTokenAccount ?? null,
      isWritable: false,
    },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("authority", accounts.authority),
      getAccountMeta("nameRecord", accounts.nameRecord),
      getAccountMeta("nftTokenAccount", accounts.nftTokenAccount),
    ],
    data: getSetResolverInstructionDataEncoder().encode(
      args as SetResolverInstructionDataArgs,
    ),
    programAddress,
  } as SetResolverInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountNameRecord,
    TAccountNftTokenAccount
  >);
}

export type ParsedSetResolverInstruction<
  TProgram extends string = typeof SOLANS_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    authority: TAccountMetas[0];
    nameRecord: TAccountMetas[1];
    /**
     * Proof of NFT holdership, required only while the name is tokenized: the
     * holder (not the stale `owner`) manages the resolver attribute — a dynamic
     * NFT (spec §6.1). Omitted (`None`) for non-tokenized names.
     */
    nftTokenAccount?: TAccountMetas[2] | undefined;
  };
  data: SetResolverInstructionData;
};

export function parseSetResolverInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedSetResolverInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 3,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  const getNextOptionalAccount = () => {
    const accountMeta = getNextAccount();
    return accountMeta.address === SOLANS_PROGRAM_ADDRESS
      ? undefined
      : accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      authority: getNextAccount(),
      nameRecord: getNextAccount(),
      nftTokenAccount: getNextOptionalAccount(),
    },
    data: getSetResolverInstructionDataDecoder().decode(instruction.data),
  };
}
