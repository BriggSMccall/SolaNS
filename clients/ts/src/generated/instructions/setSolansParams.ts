/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU16Decoder,
  getU16Encoder,
  getU64Decoder,
  getU64Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/kit/program-client-core";
import { findConfigPda } from "../pdas";
import { SOLANS_PROGRAM_ADDRESS } from "../programs";

export const SET_SOLANS_PARAMS_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([91, 68, 181, 191, 248, 118, 235, 102]);

export function getSetSolansParamsDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    SET_SOLANS_PARAMS_DISCRIMINATOR,
  );
}

export type SetSolansParamsInstruction<
  TProgram extends string = typeof SOLANS_PROGRAM_ADDRESS,
  TAccountAdmin extends string | AccountMeta<string> = string,
  TAccountConfig extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAdmin extends string
        ? ReadonlySignerAccount<TAccountAdmin> &
            AccountSignerMeta<TAccountAdmin>
        : TAccountAdmin,
      TAccountConfig extends string
        ? WritableAccount<TAccountConfig>
        : TAccountConfig,
      ...TRemainingAccounts,
    ]
  >;

export type SetSolansParamsInstructionData = {
  discriminator: ReadonlyUint8Array;
  solansRate: bigint;
  solansDiscountBps: number;
};

export type SetSolansParamsInstructionDataArgs = {
  solansRate: number | bigint;
  solansDiscountBps: number;
};

export function getSetSolansParamsInstructionDataEncoder(): FixedSizeEncoder<SetSolansParamsInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["solansRate", getU64Encoder()],
      ["solansDiscountBps", getU16Encoder()],
    ]),
    (value) => ({ ...value, discriminator: SET_SOLANS_PARAMS_DISCRIMINATOR }),
  );
}

export function getSetSolansParamsInstructionDataDecoder(): FixedSizeDecoder<SetSolansParamsInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["solansRate", getU64Decoder()],
    ["solansDiscountBps", getU16Decoder()],
  ]);
}

export function getSetSolansParamsInstructionDataCodec(): FixedSizeCodec<
  SetSolansParamsInstructionDataArgs,
  SetSolansParamsInstructionData
> {
  return combineCodec(
    getSetSolansParamsInstructionDataEncoder(),
    getSetSolansParamsInstructionDataDecoder(),
  );
}

export type SetSolansParamsAsyncInput<
  TAccountAdmin extends string = string,
  TAccountConfig extends string = string,
> = {
  admin: TransactionSigner<TAccountAdmin>;
  config?: Address<TAccountConfig>;
  solansRate: SetSolansParamsInstructionDataArgs["solansRate"];
  solansDiscountBps: SetSolansParamsInstructionDataArgs["solansDiscountBps"];
};

export async function getSetSolansParamsInstructionAsync<
  TAccountAdmin extends string,
  TAccountConfig extends string,
  TProgramAddress extends Address = typeof SOLANS_PROGRAM_ADDRESS,
>(
  input: SetSolansParamsAsyncInput<TAccountAdmin, TAccountConfig>,
  config?: { programAddress?: TProgramAddress },
): Promise<
  SetSolansParamsInstruction<TProgramAddress, TAccountAdmin, TAccountConfig>
> {
  // Program address.
  const programAddress = config?.programAddress ?? SOLANS_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    admin: { value: input.admin ?? null, isWritable: false },
    config: { value: input.config ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.config.value) {
    accounts.config.value = await findConfigPda();
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("admin", accounts.admin),
      getAccountMeta("config", accounts.config),
    ],
    data: getSetSolansParamsInstructionDataEncoder().encode(
      args as SetSolansParamsInstructionDataArgs,
    ),
    programAddress,
  } as SetSolansParamsInstruction<
    TProgramAddress,
    TAccountAdmin,
    TAccountConfig
  >);
}

export type SetSolansParamsInput<
  TAccountAdmin extends string = string,
  TAccountConfig extends string = string,
> = {
  admin: TransactionSigner<TAccountAdmin>;
  config: Address<TAccountConfig>;
  solansRate: SetSolansParamsInstructionDataArgs["solansRate"];
  solansDiscountBps: SetSolansParamsInstructionDataArgs["solansDiscountBps"];
};

export function getSetSolansParamsInstruction<
  TAccountAdmin extends string,
  TAccountConfig extends string,
  TProgramAddress extends Address = typeof SOLANS_PROGRAM_ADDRESS,
>(
  input: SetSolansParamsInput<TAccountAdmin, TAccountConfig>,
  config?: { programAddress?: TProgramAddress },
): SetSolansParamsInstruction<TProgramAddress, TAccountAdmin, TAccountConfig> {
  // Program address.
  const programAddress = config?.programAddress ?? SOLANS_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    admin: { value: input.admin ?? null, isWritable: false },
    config: { value: input.config ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("admin", accounts.admin),
      getAccountMeta("config", accounts.config),
    ],
    data: getSetSolansParamsInstructionDataEncoder().encode(
      args as SetSolansParamsInstructionDataArgs,
    ),
    programAddress,
  } as SetSolansParamsInstruction<
    TProgramAddress,
    TAccountAdmin,
    TAccountConfig
  >);
}

export type ParsedSetSolansParamsInstruction<
  TProgram extends string = typeof SOLANS_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    admin: TAccountMetas[0];
    config: TAccountMetas[1];
  };
  data: SetSolansParamsInstructionData;
};

export function parseSetSolansParamsInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedSetSolansParamsInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 2,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { admin: getNextAccount(), config: getNextAccount() },
    data: getSetSolansParamsInstructionDataDecoder().decode(instruction.data),
  };
}
