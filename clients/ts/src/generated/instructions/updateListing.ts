/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getI64Decoder,
  getI64Encoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/kit/program-client-core";
import { SOLANS_PROGRAM_ADDRESS } from "../programs";

export const UPDATE_LISTING_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  192, 174, 210, 68, 116, 40, 242, 253,
]);

export function getUpdateListingDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    UPDATE_LISTING_DISCRIMINATOR,
  );
}

export type UpdateListingInstruction<
  TProgram extends string = typeof SOLANS_PROGRAM_ADDRESS,
  TAccountSeller extends string | AccountMeta<string> = string,
  TAccountListing extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountSeller extends string
        ? ReadonlySignerAccount<TAccountSeller> &
            AccountSignerMeta<TAccountSeller>
        : TAccountSeller,
      TAccountListing extends string
        ? WritableAccount<TAccountListing>
        : TAccountListing,
      ...TRemainingAccounts,
    ]
  >;

export type UpdateListingInstructionData = {
  discriminator: ReadonlyUint8Array;
  price: bigint;
  durationSeconds: bigint;
};

export type UpdateListingInstructionDataArgs = {
  price: number | bigint;
  durationSeconds: number | bigint;
};

export function getUpdateListingInstructionDataEncoder(): FixedSizeEncoder<UpdateListingInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["price", getU64Encoder()],
      ["durationSeconds", getI64Encoder()],
    ]),
    (value) => ({ ...value, discriminator: UPDATE_LISTING_DISCRIMINATOR }),
  );
}

export function getUpdateListingInstructionDataDecoder(): FixedSizeDecoder<UpdateListingInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["price", getU64Decoder()],
    ["durationSeconds", getI64Decoder()],
  ]);
}

export function getUpdateListingInstructionDataCodec(): FixedSizeCodec<
  UpdateListingInstructionDataArgs,
  UpdateListingInstructionData
> {
  return combineCodec(
    getUpdateListingInstructionDataEncoder(),
    getUpdateListingInstructionDataDecoder(),
  );
}

export type UpdateListingInput<
  TAccountSeller extends string = string,
  TAccountListing extends string = string,
> = {
  seller: TransactionSigner<TAccountSeller>;
  listing: Address<TAccountListing>;
  price: UpdateListingInstructionDataArgs["price"];
  durationSeconds: UpdateListingInstructionDataArgs["durationSeconds"];
};

export function getUpdateListingInstruction<
  TAccountSeller extends string,
  TAccountListing extends string,
  TProgramAddress extends Address = typeof SOLANS_PROGRAM_ADDRESS,
>(
  input: UpdateListingInput<TAccountSeller, TAccountListing>,
  config?: { programAddress?: TProgramAddress },
): UpdateListingInstruction<TProgramAddress, TAccountSeller, TAccountListing> {
  // Program address.
  const programAddress = config?.programAddress ?? SOLANS_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    seller: { value: input.seller ?? null, isWritable: false },
    listing: { value: input.listing ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("seller", accounts.seller),
      getAccountMeta("listing", accounts.listing),
    ],
    data: getUpdateListingInstructionDataEncoder().encode(
      args as UpdateListingInstructionDataArgs,
    ),
    programAddress,
  } as UpdateListingInstruction<
    TProgramAddress,
    TAccountSeller,
    TAccountListing
  >);
}

export type ParsedUpdateListingInstruction<
  TProgram extends string = typeof SOLANS_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    seller: TAccountMetas[0];
    listing: TAccountMetas[1];
  };
  data: UpdateListingInstructionData;
};

export function parseUpdateListingInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedUpdateListingInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 2,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { seller: getNextAccount(), listing: getNextAccount() },
    data: getUpdateListingInstructionDataDecoder().decode(instruction.data),
  };
}
