/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  addDecoderSizePrefix,
  addEncoderSizePrefix,
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getUtf8Decoder,
  getUtf8Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  getNonNullResolvedInstructionInput,
  type ResolvedInstructionAccount,
} from "@solana/kit/program-client-core";
import { findNameRecordPda } from "../pdas";
import { SOLANS_PROGRAM_ADDRESS } from "../programs";

export const WRAP_SUBDOMAIN_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  176, 182, 82, 241, 229, 46, 229, 5,
]);

export function getWrapSubdomainDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    WRAP_SUBDOMAIN_DISCRIMINATOR,
  );
}

export type WrapSubdomainInstruction<
  TProgram extends string = typeof SOLANS_PROGRAM_ADDRESS,
  TAccountOwner extends string | AccountMeta<string> = string,
  TAccountSubdomainOwner extends string | AccountMeta<string> = string,
  TAccountParentName extends string | AccountMeta<string> = string,
  TAccountNameRecord extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountOwner extends string
        ? WritableSignerAccount<TAccountOwner> &
            AccountSignerMeta<TAccountOwner>
        : TAccountOwner,
      TAccountSubdomainOwner extends string
        ? ReadonlyAccount<TAccountSubdomainOwner>
        : TAccountSubdomainOwner,
      TAccountParentName extends string
        ? ReadonlyAccount<TAccountParentName>
        : TAccountParentName,
      TAccountNameRecord extends string
        ? WritableAccount<TAccountNameRecord>
        : TAccountNameRecord,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type WrapSubdomainInstructionData = {
  discriminator: ReadonlyUint8Array;
  label: string;
  nameHash: ReadonlyUint8Array;
};

export type WrapSubdomainInstructionDataArgs = {
  label: string;
  nameHash: ReadonlyUint8Array;
};

export function getWrapSubdomainInstructionDataEncoder(): Encoder<WrapSubdomainInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["label", addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
      ["nameHash", fixEncoderSize(getBytesEncoder(), 32)],
    ]),
    (value) => ({ ...value, discriminator: WRAP_SUBDOMAIN_DISCRIMINATOR }),
  );
}

export function getWrapSubdomainInstructionDataDecoder(): Decoder<WrapSubdomainInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["label", addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
    ["nameHash", fixDecoderSize(getBytesDecoder(), 32)],
  ]);
}

export function getWrapSubdomainInstructionDataCodec(): Codec<
  WrapSubdomainInstructionDataArgs,
  WrapSubdomainInstructionData
> {
  return combineCodec(
    getWrapSubdomainInstructionDataEncoder(),
    getWrapSubdomainInstructionDataDecoder(),
  );
}

export type WrapSubdomainAsyncInput<
  TAccountOwner extends string = string,
  TAccountSubdomainOwner extends string = string,
  TAccountParentName extends string = string,
  TAccountNameRecord extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  owner: TransactionSigner<TAccountOwner>;
  subdomainOwner: Address<TAccountSubdomainOwner>;
  parentName: Address<TAccountParentName>;
  nameRecord?: Address<TAccountNameRecord>;
  systemProgram?: Address<TAccountSystemProgram>;
  label: WrapSubdomainInstructionDataArgs["label"];
  nameHash: WrapSubdomainInstructionDataArgs["nameHash"];
};

export async function getWrapSubdomainInstructionAsync<
  TAccountOwner extends string,
  TAccountSubdomainOwner extends string,
  TAccountParentName extends string,
  TAccountNameRecord extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof SOLANS_PROGRAM_ADDRESS,
>(
  input: WrapSubdomainAsyncInput<
    TAccountOwner,
    TAccountSubdomainOwner,
    TAccountParentName,
    TAccountNameRecord,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  WrapSubdomainInstruction<
    TProgramAddress,
    TAccountOwner,
    TAccountSubdomainOwner,
    TAccountParentName,
    TAccountNameRecord,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? SOLANS_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    owner: { value: input.owner ?? null, isWritable: true },
    subdomainOwner: { value: input.subdomainOwner ?? null, isWritable: false },
    parentName: { value: input.parentName ?? null, isWritable: false },
    nameRecord: { value: input.nameRecord ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.nameRecord.value) {
    accounts.nameRecord.value = await findNameRecordPda({
      nameHash: getNonNullResolvedInstructionInput("nameHash", args.nameHash),
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("owner", accounts.owner),
      getAccountMeta("subdomainOwner", accounts.subdomainOwner),
      getAccountMeta("parentName", accounts.parentName),
      getAccountMeta("nameRecord", accounts.nameRecord),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getWrapSubdomainInstructionDataEncoder().encode(
      args as WrapSubdomainInstructionDataArgs,
    ),
    programAddress,
  } as WrapSubdomainInstruction<
    TProgramAddress,
    TAccountOwner,
    TAccountSubdomainOwner,
    TAccountParentName,
    TAccountNameRecord,
    TAccountSystemProgram
  >);
}

export type WrapSubdomainInput<
  TAccountOwner extends string = string,
  TAccountSubdomainOwner extends string = string,
  TAccountParentName extends string = string,
  TAccountNameRecord extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  owner: TransactionSigner<TAccountOwner>;
  subdomainOwner: Address<TAccountSubdomainOwner>;
  parentName: Address<TAccountParentName>;
  nameRecord: Address<TAccountNameRecord>;
  systemProgram?: Address<TAccountSystemProgram>;
  label: WrapSubdomainInstructionDataArgs["label"];
  nameHash: WrapSubdomainInstructionDataArgs["nameHash"];
};

export function getWrapSubdomainInstruction<
  TAccountOwner extends string,
  TAccountSubdomainOwner extends string,
  TAccountParentName extends string,
  TAccountNameRecord extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof SOLANS_PROGRAM_ADDRESS,
>(
  input: WrapSubdomainInput<
    TAccountOwner,
    TAccountSubdomainOwner,
    TAccountParentName,
    TAccountNameRecord,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): WrapSubdomainInstruction<
  TProgramAddress,
  TAccountOwner,
  TAccountSubdomainOwner,
  TAccountParentName,
  TAccountNameRecord,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? SOLANS_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    owner: { value: input.owner ?? null, isWritable: true },
    subdomainOwner: { value: input.subdomainOwner ?? null, isWritable: false },
    parentName: { value: input.parentName ?? null, isWritable: false },
    nameRecord: { value: input.nameRecord ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("owner", accounts.owner),
      getAccountMeta("subdomainOwner", accounts.subdomainOwner),
      getAccountMeta("parentName", accounts.parentName),
      getAccountMeta("nameRecord", accounts.nameRecord),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getWrapSubdomainInstructionDataEncoder().encode(
      args as WrapSubdomainInstructionDataArgs,
    ),
    programAddress,
  } as WrapSubdomainInstruction<
    TProgramAddress,
    TAccountOwner,
    TAccountSubdomainOwner,
    TAccountParentName,
    TAccountNameRecord,
    TAccountSystemProgram
  >);
}

export type ParsedWrapSubdomainInstruction<
  TProgram extends string = typeof SOLANS_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    owner: TAccountMetas[0];
    subdomainOwner: TAccountMetas[1];
    parentName: TAccountMetas[2];
    nameRecord: TAccountMetas[3];
    systemProgram: TAccountMetas[4];
  };
  data: WrapSubdomainInstructionData;
};

export function parseWrapSubdomainInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedWrapSubdomainInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 5) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 5,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      owner: getNextAccount(),
      subdomainOwner: getNextAccount(),
      parentName: getNextAccount(),
      nameRecord: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getWrapSubdomainInstructionDataDecoder().decode(instruction.data),
  };
}
